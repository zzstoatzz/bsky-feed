# Bluesky Credentials & Service Info
HOSTNAME="example.com" # Your domain, e.g., example.com or my-feed.example.com
HANDLE=""            # Your Bluesky handle, e.g., yourname.bsky.social
PASSWORD=""          # Your Bluesky app password

# Feed Publishing Info (used by publish_feed.py)
RECORD_NAME="test-feed"      # Short name for your feed (used in URI, e.g., test-feed)
DISPLAY_NAME="My Test Feed" # How your feed is displayed in Bluesky clients
#FEED_DESCRIPTION="This is a test feed powered by the ATProto Feed Generator."
#AVATAR_PATH="./path/to/your/avatar.png" # Optional: Path to an image for the feed avatar

# Server Configuration
#SERVICE_DID="" # Optional: Defaults to did:web:<HOSTNAME>
#LISTEN_HOST="0.0.0.0"
#LISTEN_PORT=8080
#LOG_LEVEL="INFO"

# Feed Behavior
#IGNORE_ARCHIVED_POSTS=False # Set to True to ignore posts older than a day
#IGNORE_REPLY_POSTS=False    # Set to True to ignore replies

# Custom Filter Function (Optional)
# CUSTOM_FILTER_FUNCTION="my_custom_filters.my_filter_function" # Example: Python import path to your custom filter function.
# The function should accept two arguments: (record, created_post) and return True to include the post, False otherwise.
# `record` is an atproto.models.AppBskyFeedPost.Record object.
# `created_post` is a dict with post metadata like 'uri' and 'cid'.
# Ensure the module (e.g., my_custom_filters.py) is in your PYTHONPATH or the project root.

# Database location
#DATABASE_URI="feed_database.db"